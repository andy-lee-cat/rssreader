# 251212

## 添加ai总结功能

我修改了数据库表entries，添加了ai_summary字段，我现在打算把前面的逻辑改成如果数据库中有ai_summary字段，则直接显示，如果没有则按照先前的逻辑更新ai_summary字段，并流式显示。

流式显示这个逻辑应该需要调用ai服务，目前应该抽象成一个输入是rss内容，流式输出是ai总结的函数？


### ai回复

● 代码编译成功！以下是完整的修改总结：

  修改的文件

  新增文件

  1. internal/ai/ai.go - AI 总结服务接口定义
    - SummaryProvider 接口：定义 GenerateSummary 方法（流式输出）
    - Config 配置结构体
  2. internal/ai/mock.go - Mock 实现
    - 返回前 100 个字符作为模拟总结
    - 支持流式输出（逐字符）
  3. internal/ai/provider.go - Provider 工厂
    - NewProvider() 根据配置创建 provider
    - GetDefaultProvider() 获取默认 provider
  4. internal/ui/entry_ai_summary.go - SSE 流式 API
    - 检查数据库是否有现成总结
    - 有则直接流式返回，无则调用 AI 服务生成
    - 生成后保存到数据库

  修改的文件

  1. internal/storage/entry_query_builder.go
    - SELECT 添加 e.ai_summary
    - Scan 添加 &entry.AISummary
  2. internal/storage/entry.go
    - 添加 UpdateEntryAISummary() 方法
  3. internal/ui/ui.go
    - 添加路由 /entry/ai-summary/{entryID}
  4. internal/template/templates/views/entry.html
    - 按钮添加 data-ai-summary-url 属性
  5. internal/ui/static/js/app.js
    - 使用 SSE (EventSource) 接收流式数据

  架构设计

  ┌─────────────────┐     SSE Stream      ┌──────────────────┐
  │   Frontend      │ ◄────────────────── │   Backend API    │
  │   (EventSource) │                     │ /entry/ai-summary│
  └─────────────────┘                     └────────┬─────────┘
                                                   │
                              ┌────────────────────┴───────────────────┐
                              │                                        │
                      ┌───────▼───────┐
  ┌────────▼────────┐
                      │   Database    │                       │   AI
  Provider   │
                      │ (ai_summary)  │                       │
  (SummaryProvider)
                      └───────────────┘
  └─────────────────┘
                                                                      │

  ┌─────────────────────┼─────────────────────┐
                                                │                     │
                  │
                                         ┌──────▼──────┐
  ┌───────▼───────┐      ┌──────▼──────┐
                                         │    Mock     │      │    OpenAI
    │      │    Claude   │
                                         │  (当前实现) │      │   (待实现)
   │      │   (待实现)  │
                                         └─────────────┘
  └───────────────┘      └─────────────┘

  后续接入真实 AI 服务

  只需在 internal/ai/ 目录下添加新的 provider 实现，例如 openai.go：

  type OpenAIProvider struct {
      apiKey string
      model  string
      apiKey string
      model  string
  }

  func (p *OpenAIProvider) GenerateSummary(ctx context.Context, content
  string) (<-chan string, error) {
      ch := make(chan string)
      go func() {
          defer close(ch)
          // 调用 OpenAI API，流式返回结果
          // 每次收到 chunk 时: ch <- chunk
      }()
      return ch, nil
  }

  然后在 provider.go 的 NewProvider() 中添加对应的 case。

## 前端修复

你没有修复这个问题：刚打开一个entry页，ai总结应该是隐藏的（等价于现在的实现，然后手动点击叉号）

## 前端修复2

现在在ai流式输出的过程中，点击点击ai总结按钮，内容会正确隐藏，并且显示加载中。但是等生成完毕后，ai总结按钮仍旧显示加载中。似乎在隐藏状态生成，会错过提示转化加载中为ai总结的信号，请你检查一下。

## 学习调用结构

在你的帮助下完完成了开发，但是我对于近期两次修改的内容理解不深入。当前提交id：c761c3a00c12599bd568ef1a239007cb36584ebc，两次前提交id：dc12713be1dddd4815e0e085ae3954112db18901。

请你在learning/路径下编写最简的逻辑，来说明这个项目涉及到刚刚修改的逻辑的调用结构。不用还原ai调用之类的，我只是想知道js文件，html文件，go文件这三者他们之间是怎么协作的。给个很简单的可以运行的例子就好。

## 拆分结构

我把你的文件放到learning/251212/下面了，你拆分下结构，把html，js，go文件拆成三个文件，并正常运行，还是放到这个路径下